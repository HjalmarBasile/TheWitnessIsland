<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_height">

    <android.support.v7.widget.CardView xmlns:card_view="http://schemas.android.com/apk/res-auto"
        style="@style/card_view_app"
        card_view:cardBackgroundColor="@color/card_view_background"
        card_view:cardCornerRadius="@dimen/card_view_corner_radius">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:id="@+id/place_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="0"
                android:fontFamily="sans-serif-smallcaps"
                android:gravity="center"
                android:textAllCaps="true"
                android:textColor="@color/colorPrimary"
                tools:text="@string/tools_text_island_place_name" />

            <ImageView
                android:id="@+id/place_image"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_gravity="center"
                android:layout_weight="1"
                android:contentDescription="@string/island_place_picture"
                android:scaleType="centerCrop"
                tools:src="@drawable/area_shady_trees" />

        </LinearLayout>

    </android.support.v7.widget.CardView>

</RelativeLayout>
